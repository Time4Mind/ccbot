# Operator-private notes

This directory is git-ignored except for this README.

Use it for any deployment recipe that references your specific
infrastructure — real IPs, server names, SSH key paths, internal
hostnames, VPN entry points, etc. Nothing else in this folder is
ever committed.

If you want to share a deployment pattern publicly, write a generic
version (with placeholders) in `doc/deploy.md` instead.
